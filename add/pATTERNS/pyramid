#include <stdio.h>

int main() {
    int i,j,n=3;
    for(i=0;i<n;i++)
    {
        for(j=0;j<(2*n-i-1);j++)
        {
             if(j<i)
             printf(" ");
            else
            printf("*");
        }
        printf("\n");
    }
    printf("Hello world");

    return 0;
}
